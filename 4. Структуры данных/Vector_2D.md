
### Задача: Реализовать структуру "Двумерный вектор" (Vector2D)

Вам нужно создать структуру для двумерного вектора с полями `x` и `y`. Реализовать функции:

- `create(x, y)` — создаёт вектор.
- `add(a, b)` — сложение векторов.
- `sub(a, b)` — вычитание.
- `mul(v, scalar)` — умножение на скаляр.
- `dot(a, b)` — скалярное произведение.
- `cross(a, b)` — векторное произведение.
- `length(v)` — длина вектора.
- `normalize(v)` — возвращает нормализованный вектор.
- `to_str(v)` — строковое представление вида `(x, y)`.

**Структура файла `vector.py`:**

```python
class Vector:
    pass

def create(x, y):
    # ... реализация
    pass

# ... другие функции
```

**Пример использования:**

```python
v1 = create(3, 4)
v2 = create(1, 2)
print(to_str(add(v1, v2)))   # (4, 6)
print(length(v1))            # 5.0
```

---
